apiVersion: v1
kind: Service
metadata:
  name: awpdwe
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "awpdwe"
    port: 8080
  selector:
    app: awpdwe
