apiVersion: v1
kind: Service
metadata:
  name: sdwej
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "sdwej"
    port: 8080
  selector:
    app: sdwej
