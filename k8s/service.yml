apiVersion: v1
kind: Service
metadata:
  name: uijgeodg
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "uijgeodg"
    port: 8080
  selector:
    app: uijgeodg
